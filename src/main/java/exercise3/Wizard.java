package exercise3;

public class Wizard extends Player{
/*The method should reduce the number of lives by one and restore the player’s
health to its original state.
**/
    public Wizard(int health, int strength, int lives) {
        super(health, strength, lives);
    }
}
